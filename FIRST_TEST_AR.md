# أول اختبار فعلي — GeoAnomaly Pro v1.5

## ما تم إصلاحه

- الإحداثيات اليدوية أصبحت ظاهرة في أعلى لوحة AOI.
- يمكن إدخال Latitude/Longitude مباشرة.
- يمكن النقر على الخريطة لتعيين الإحداثيات.
- الخريطة تستخدم طبقة Satellite حقيقية مع OpenStreetMap كبديل.
- Earth Engine له تحقق فعلي عبر طلب API، وليس مجرد علامة اتصال.
- يوجد وضع تطوير محلي يستخدم اعتماد Earth Engine الموجود على هذا الكمبيوتر فقط.
- في الإنتاج يبقى OAuth لكل مستخدم هو المسار المعتمد.
- التحليل يستخدم سياقًا مكانيًا حقيقيًا أكبر عند فحص أهداف صغيرة، ثم يسمح بالترشيح للأهداف داخل AOI فقط.
- لا يتم إنشاء نتائج أو إحداثيات أو درجات باستخدام random.
- Earth Engine Python state محمي بقفل التحليل محليًا لتجنب خلط حسابات المستخدمين أثناء الاختبار.

## التشغيل

1. فك الضغط باستخدام Extract All.
2. لا تشغل BAT من داخل WinRAR.
3. شغّل `START_GEOANOMALY.bat`.
4. افتح `http://127.0.0.1:5500/`.
5. سجل الدخول.
6. تحقق من ظهور الإحداثيات يدويًا.
7. اضغط `ربط هذا الجهاز` إذا كان OAuth غير مضبوط.

## Earth Engine المحلي

هذا المسار للاختبار على جهاز المطور فقط. يجب أن تكون قد نفذت مرة واحدة:

`earthengine authenticate`

واخترت مشروع Earth Engine الصحيح.

إذا نجح الاتصال ستظهر:

`Earth Engine Local`

ثم يصبح زر الفحص فعالًا.

## الإنتاج

لا تستخدم Local Developer Mode على خادم عام.

اضبط:

`ALLOW_LOCAL_EE_AUTH=false`

ثم أنشئ OAuth Client لتطبيق الويب واضبط:

`GOOGLE_OAUTH_CLIENT_ID`
`GOOGLE_OAUTH_CLIENT_SECRET`
`GOOGLE_OAUTH_REDIRECT_URI`

كل مستخدم يربط حساب Google/Earth Engine الخاص به، ولا نستخدم حسابًا مشتركًا.
